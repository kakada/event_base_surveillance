#log-text-field.modal.fade{'aria-hidden' => 'true', 'aria-labelledby' => 'modal-label', :role => 'dialog', :tabindex => '-1'}
  .modal-dialog{:role => 'document'}
    .modal-content
      .modal-header
        %h5#modal-label.modal-title Event Log
        %button.close{'aria-label' => 'Close', 'data-dismiss' => 'modal', :type => 'button'}
          %span{'aria-hidden' => 'true'} ×
      .modal-body
        log text field
        / %canvas#myChart{'data-event-logs' => @event.logs.to_json, 'data-tracking-number-fields' => @event.milestone.fields.tracking.number.pluck(:code), 'data-tracking-text-fields' => @event.milestone.fields.tracking.text.pluck(:code)}

        / https://freefrontend.com/css-timelines/
